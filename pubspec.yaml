name: linkify_plus
description: Turns text URLs and emails into clickable inline links in text for Flutter.
version: 6.0.4
homepage: https://github.com/Gaurav-CareMonitor/linkify_plus

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.2 
  test: ^1.26.3
  flutter_test:
    sdk: flutter

